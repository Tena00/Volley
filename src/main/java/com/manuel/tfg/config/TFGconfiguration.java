/*package com.manuel.tfg.config;


import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Configuration;


import lombok.Data;

@Configuration
@Data
public class TFGconfiguration {
    @Value("${teamstats.swagger.title}")
    private String swaggerTitle;

    @Value("${teamstats.swagger.description}")
    private String swaggerDescription;

    @Value("${teamstats.swagger.version}")
    private String swaggerVersion;

    @Value("${teamstats.swagger.contact-name}")
    private String swaggerContactName;

    @Value("${teamstats.swagger.contact-email}")
    private String swaggerContactEmail;
}
*/